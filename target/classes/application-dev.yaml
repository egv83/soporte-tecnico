server:
  port: 8081
spring:
  application:
    name: api-soporte-tecnico
  datasource:
    url: jdbc:mysql://localhost:3306/db_soporte_tecnico_v1?createDatabaseIfNotExist=true
    username: root
    password: esteban83
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    defer-datasource-initialization: true
    hibernate:
      #      create,create-drop, update, none
      ddl-auto: none
      dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true
  sql:
    init:
#      always never
      mode: always
      schema-locations: classpath:db/mysql/automatico/schema.sql
#      data-locations: classpath:db/mysql/automatico/data.sql
